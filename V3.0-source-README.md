# 测试能力成熟度看板 V3.0（源码）

## 目录结构
- `V3.0-source/`：完整源码（Flask 后端 + 静态前端 + SQLite + Windows 离线依赖包）
  - `app.py` / `run_server.py`：后端（Flask + waitress）
  - `static/index.html`：前端单页
  - `packages/`：离线 wheel（Windows 32/64 位）
  - `data/maturity.db`：初始数据库
  - `start.bat` / `start.ps1`：Windows 启动脚本（自动建 venv + 离线装依赖 + 启动）
  - `README.txt`：完整操作说明
- `测试能力成熟度-V3.0-在线服务完整版.zip`：同内容的交付包（SHA `ab70e887…`）

## 快速启动（Windows）
1. 装好 Windows CPython 3.10–3.13（32/64 位均可，勾选 Add to PATH）
2. 双击 `V3.0-source/start.bat`
3. 浏览器访问 http://127.0.0.1:5000 ，登录 admin / 123qweasd（首次请改密）

## V3.0 变更
1. 科组新增 9月/12月预期达成（expectedSep/expectedDec，与实际达成互不影响、不参与统计）
2. 三级权限：管理员全权 / 能力Owner 仅能力属性 / 业务 仅达成与预期
3. 业务线 G 雷达轴标签显示领域得分、总分移出雷达图
（更早版本变更见应用内"版本记录"页）
